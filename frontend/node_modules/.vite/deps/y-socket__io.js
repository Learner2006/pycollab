import { t as lookup } from "./esm-D8A8Bn5L.js";
import { B as applyUpdate, Qt as toBase64, Xt as createUint8ArrayFromArrayBuffer, Zt as fromBase64, en as offChange, hn as setIfUndefined, ht as encodeStateVector, m as Doc, mn as create, nn as varStorage, pn as Observable, pt as encodeStateAsUpdate, tn as onChange } from "./yjs-Be4UN7oe.js";
import { i as removeAwarenessStates, n as applyAwarenessUpdate, r as encodeAwarenessUpdate, t as Awareness } from "./awareness-Dodbuurt.js";
//#region node_modules/lib0/broadcastchannel.js
/**
* Helpers for cross-tab communication using broadcastchannel with LocalStorage fallback.
*
* ```js
* // In browser window A:
* broadcastchannel.subscribe('my events', data => console.log(data))
* broadcastchannel.publish('my events', 'Hello world!') // => A: 'Hello world!' fires synchronously in same tab
*
* // In browser window B:
* broadcastchannel.publish('my events', 'hello from tab B') // => A: 'hello from tab B'
* ```
*
* @module broadcastchannel
*/
/**
* @typedef {Object} Channel
* @property {Set<function(any, any):any>} Channel.subs
* @property {any} Channel.bc
*/
/**
* @type {Map<string, Channel>}
*/
var channels = /* @__PURE__ */ new Map();
/* c8 ignore start */
var LocalStoragePolyfill = class {
	/**
	* @param {string} room
	*/
	constructor(room) {
		this.room = room;
		/**
		* @type {null|function({data:Uint8Array}):void}
		*/
		this.onmessage = null;
		/**
		* @param {any} e
		*/
		this._onChange = (e) => e.key === room && this.onmessage !== null && this.onmessage({ data: fromBase64(e.newValue || "") });
		onChange(this._onChange);
	}
	/**
	* @param {ArrayBuffer} buf
	*/
	postMessage(buf) {
		varStorage.setItem(this.room, toBase64(createUint8ArrayFromArrayBuffer(buf)));
	}
	close() {
		offChange(this._onChange);
	}
};
/* c8 ignore stop */
/* c8 ignore next */
var BC = typeof BroadcastChannel === "undefined" ? LocalStoragePolyfill : BroadcastChannel;
/**
* @param {string} room
* @return {Channel}
*/
var getChannel = (room) => setIfUndefined(channels, room, () => {
	const subs = create();
	const bc = new BC(room);
	/**
	* @param {{data:ArrayBuffer}} e
	*/
	/* c8 ignore next */
	bc.onmessage = (e) => subs.forEach((sub) => sub(e.data, "broadcastchannel"));
	return {
		bc,
		subs
	};
});
/**
* Subscribe to global `publish` events.
*
* @function
* @param {string} room
* @param {function(any, any):any} f
*/
var subscribe = (room, f) => {
	getChannel(room).subs.add(f);
	return f;
};
/**
* Unsubscribe from `publish` global events.
*
* @function
* @param {string} room
* @param {function(any, any):any} f
*/
var unsubscribe = (room, f) => {
	const channel = getChannel(room);
	const unsubscribed = channel.subs.delete(f);
	if (unsubscribed && channel.subs.size === 0) {
		channel.bc.close();
		channels.delete(room);
	}
	return unsubscribed;
};
/**
* Publish data to all subscribers (including subscribers on this tab)
*
* @function
* @param {string} room
* @param {any} data
* @param {any} [origin]
*/
var publish = (room, data, origin = null) => {
	const c = getChannel(room);
	c.bc.postMessage(data);
	c.subs.forEach((sub) => sub(data, origin));
};
//#endregion
//#region node_modules/y-socket.io/dist/index.mjs
var h = class extends Observable {
	constructor(e, t, o = new Doc(), { autoConnect: c = !0, awareness: i = new Awareness(o), resyncInterval: l = -1, disableBc: p = !1, auth: y = {} }, d = void 0) {
		super();
		this.bcconnected = !1;
		this._synced = !1;
		this.resyncInterval = null;
		this.initSyncListeners = () => {
			this.socket.on("sync-step-1", (e, t) => {
				t(encodeStateAsUpdate(this.doc, new Uint8Array(e))), this.synced = !0;
			}), this.socket.on("sync-update", this.onSocketSyncUpdate);
		};
		this.initAwarenessListeners = () => {
			this.socket.on("awareness-update", (e) => {
				applyAwarenessUpdate(this.awareness, new Uint8Array(e), this);
			});
		};
		this.initSystemListeners = () => {
			typeof window != "undefined" ? window.addEventListener("beforeunload", this.beforeUnloadHandler) : typeof process != "undefined" && process.on("exit", this.beforeUnloadHandler);
		};
		this.onSocketConnection = (e = -1) => {
			this.emit("status", [{ status: "connected" }]), this.socket.emit("sync-step-1", encodeStateVector(this.doc), (t) => {
				applyUpdate(this.doc, new Uint8Array(t), this);
			}), this.awareness.getLocalState() !== null && this.socket.emit("awareness-update", encodeAwarenessUpdate(this.awareness, [this.doc.clientID])), e > 0 && (this.resyncInterval = setInterval(() => {
				this.socket.disconnected || this.socket.emit("sync-step-1", encodeStateVector(this.doc), (t) => {
					applyUpdate(this.doc, new Uint8Array(t), this);
				});
			}, e));
		};
		this.onSocketDisconnection = (e) => {
			this.emit("connection-close", [e, this]), this.synced = !1, removeAwarenessStates(this.awareness, Array.from(this.awareness.getStates().keys()).filter((t) => t !== this.doc.clientID), this), this.emit("status", [{ status: "disconnected" }]);
		};
		this.onSocketConnectionError = (e) => {
			this.emit("connection-error", [e, this]);
		};
		this.onUpdateDoc = (e, t) => {
			t !== this && (this.socket.emit("sync-update", e), this.bcconnected && publish(this._broadcastChannel, {
				type: "sync-update",
				data: e
			}, this));
		};
		this.onSocketSyncUpdate = (e) => {
			applyUpdate(this.doc, new Uint8Array(e), this);
		};
		this.awarenessUpdate = ({ added: e, updated: t, removed: o }, c) => {
			let i = e.concat(t).concat(o);
			this.socket.emit("awareness-update", encodeAwarenessUpdate(this.awareness, i)), this.bcconnected && publish(this._broadcastChannel, {
				type: "awareness-update",
				data: encodeAwarenessUpdate(this.awareness, i)
			}, this);
		};
		this.beforeUnloadHandler = () => {
			removeAwarenessStates(this.awareness, [this.doc.clientID], "window unload");
		};
		this.connectBc = () => {
			this.bcconnected || (subscribe(this._broadcastChannel, this.onBroadcastChannelMessage), this.bcconnected = !0), publish(this._broadcastChannel, {
				type: "sync-step-1",
				data: encodeStateVector(this.doc)
			}, this), publish(this._broadcastChannel, {
				type: "sync-step-2",
				data: encodeStateAsUpdate(this.doc)
			}, this), publish(this._broadcastChannel, {
				type: "query-awareness",
				data: null
			}, this), publish(this._broadcastChannel, {
				type: "awareness-update",
				data: encodeAwarenessUpdate(this.awareness, [this.doc.clientID])
			}, this);
		};
		this.disconnectBc = () => {
			publish(this._broadcastChannel, {
				type: "awareness-update",
				data: encodeAwarenessUpdate(this.awareness, [this.doc.clientID], /* @__PURE__ */ new Map())
			}, this), this.bcconnected && (unsubscribe(this._broadcastChannel, this.onBroadcastChannelMessage), this.bcconnected = !1);
		};
		this.onBroadcastChannelMessage = (e, t) => {
			if (t !== this && e.type.length > 0) switch (e.type) {
				case "sync-step-1":
					publish(this._broadcastChannel, {
						type: "sync-step-2",
						data: encodeStateAsUpdate(this.doc, e.data)
					}, this);
					break;
				case "sync-step-2":
					applyUpdate(this.doc, new Uint8Array(e.data), this);
					break;
				case "sync-update":
					applyUpdate(this.doc, new Uint8Array(e.data), this);
					break;
				case "query-awareness":
					publish(this._broadcastChannel, {
						type: "awareness-update",
						data: encodeAwarenessUpdate(this.awareness, Array.from(this.awareness.getStates().keys()))
					}, this);
					break;
				case "awareness-update":
					applyAwarenessUpdate(this.awareness, new Uint8Array(e.data), this);
					break;
				default: break;
			}
		};
		for (; e[e.length - 1] === "/";) e = e.slice(0, e.length - 1);
		this._url = e, this.roomName = t, this.doc = o, this.awareness = i, this._broadcastChannel = `${e}/${t}`, this.disableBc = p, this._socketIoOptions = d, this.socket = lookup(`${this.url}/yjs|${t}`, {
			autoConnect: !1,
			transports: ["websocket"],
			forceNew: !0,
			auth: y,
			...d
		}), this.doc.on("update", this.onUpdateDoc), this.socket.on("connect", () => this.onSocketConnection(l)), this.socket.on("disconnect", (r) => this.onSocketDisconnection(r)), this.socket.on("connect_error", (r) => this.onSocketConnectionError(r)), this.initSyncListeners(), this.initAwarenessListeners(), this.initSystemListeners(), i.on("update", this.awarenessUpdate), c && this.connect();
	}
	get broadcastChannel() {
		return this._broadcastChannel;
	}
	get url() {
		return this._url;
	}
	get synced() {
		return this._synced;
	}
	set synced(e) {
		this._synced !== e && (this._synced = e, this.emit("synced", [e]), this.emit("sync", [e]));
	}
	connect() {
		this.socket.connected || (this.emit("status", [{ status: "connecting" }]), this.socket.connect(), this.disableBc || this.connectBc(), this.synced = !1);
	}
	disconnect() {
		this.socket.connected && (this.disconnectBc(), this.socket.disconnect());
	}
	destroy() {
		this.resyncInterval != null && clearInterval(this.resyncInterval), this.disconnect(), typeof window != "undefined" ? window.removeEventListener("beforeunload", this.beforeUnloadHandler) : typeof process != "undefined" && process.off("exit", this.beforeUnloadHandler), this.awareness.off("update", this.awarenessUpdate), this.awareness.destroy(), this.doc.off("update", this.onUpdateDoc), super.destroy();
	}
};
//#endregion
export { h as SocketIOProvider };

//# sourceMappingURL=y-socket__io.js.map